# For the string and bytes types, x in y is True if and only if x is a
# substring of y. Empty strings are always considered to be a substring
# of any other string, so "" in "abc" will return True.

