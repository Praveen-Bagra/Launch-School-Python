# Two differences between lists and tuples.
# 1. Lists are mutable while tuples are immutable collection type.
# 2. List literal use [], while tuple literal use (). 

# Two ways that lists are tuples are similar:
# 1. Both maintain ordered collection of elements.
# 2. Both are heterogeneous. They can contain any type of object, including
#    other sequences.
# 3. In both of them, their element can be accessed through whole integer
#    indices (indexing syntax)