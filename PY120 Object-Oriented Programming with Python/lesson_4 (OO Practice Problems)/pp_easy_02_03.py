# Benefits of using object-oriented programming in Python:
#   -   We can hide implementation details and expose methods we want
#       to expose in public interface. This helps in securing our code.
#   -   We can use the same method on different objects and expect
#       consistent return values. We can define it through inheritance.
#   -   We can reuse our code through mixins, collaborators.